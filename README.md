# 🎫 데이터 기반 프리미엄 중고 거래 플랫폼 (DB Course Project)

본 프로젝트는 단순한 중고 거래 사이트가 아닌, **데이터베이스의 강력한 기능(Constraints, Triggers, JSONB Indexing)**을 활용하여 중고 거래의 고질적인 문제(부정 거래, 데이터 파편화)를 해결하는 것을 목표로 합니다.

## 🌟 핵심 차별점 (Core Features)

### 1. SKU 기반 표준 데이터 관리 (Scenario A)
*   **문제**: 기존 플랫폼은 사용자가 입력한 모호한 제목("아이폰 15 팝니다", "애플폰 15")에 의존하여 검색 정확도가 낮음.
*   **해결**: 판매자가 글을 올릴 때 DB에 저장된 **표준 상품(Standard Product)**과 매핑하도록 강제.
*   **효과**: "선물받은거 팝니다"라는 제목이어도 `SKU: SM-S9210(Galaxy S24)` 메타데이터를 통해 정확히 검색됨.

### 2. DB Trigger 기반 부정 거래/암표 방지 (Scenario B)
*   **문제**: 애플리케이션 레벨에서만 가격을 제한하면, API 우회 등을 통해 암표 거래가 가능함.
*   **해결**: PostgreSQL **Trigger**를 사용하여, `INSERT/UPDATE` 시점에 **(판매가 > 정가의 120%)**인 경우 DB 엔진 레벨에서 즉시 차단을 수행.
*   **효과**: 건전한 티켓/한정판 거래 문화 정착. UI에서 `+15%` 등의 프리미엄율이 투명하게 공개됨.

### 3. PostgreSQL JSONB를 활용한 유연한 필터링 (Scenario C)
*   **문제**: 티켓의 좌석 정보(구역, 열, 번호, 등급)나 전자기기의 스펙은 상품마다 상이하여 고정 스키마로 관리하기 어려움.
*   **해결**: `seat_info`, `specs` 컬럼을 **JSONB** 타입으로 정의하고, **GIN Index**를 적용.
*   **효과**: 스키마 변경 없이도 "A구역 VIP석" 같은 깊은 계층의 데이터를 실시간으로 고속 필터링 가능.

---

## 🛠 기술 스택 (Tech Stack)

*   **Frontend**: React (Vite), TypeScript, Tailwind CSS
*   **Backend**: Node.js, Express
*   **Database**: PostgreSQL (Railway/Render Cloud DB)
*   **Collaboration**: Git, Notion

---

## 🚀 실행 방법 (How to Run)

### 1. Backend 실행
```bash
cd backend
npm install
npm run dev
# Server runs on http://localhost:4000
```

### 2. Frontend 실행
```bash
cd frontend
npm install
npm run dev
# Client runs on http://localhost:5173
```

### 3. 주요 테스트 계정
*   **Buyer**: `user_buyer` / `1234` (Demo)
*   **Seller**: `user_seller` / `1234` (Demo)

---

## 📂 프로젝트 구조

```
/
├── backend/          # Express API Server & SQL Scripts
│   ├── seed_realistic.js  # 현실적인 더미 데이터 생성 (브랜드-모델 매핑)
│   └── ...
├── frontend/         # React Application
│   ├── src/pages/Sell.tsx # 부정 거래 방지 시나리오 UI
│   └── ...
├── Final_Presentation_Script.md # 시연 가이드
└── ...
```
